## Project Startup Notes

- If not launching the project, remember to restart it from another terminal or find an alternative to `npm start` if needed

## WSL Development Notes

- To use WSL for local development builds (especially for Linux/Mac-specific environments), use the command: `wsl -d Ubuntu`